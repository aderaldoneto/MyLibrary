{{ form_start(form, {attr: {class: 'catalog-form author-form'}}) }}
    {{ form_row(form.nome) }}
    <div class="form-actions author-form-actions">
        <a class="link-button" href="{{ path('app_autor_index') }}">Cancelar</a>
        <button type="submit">Salvar</button>
    </div>
{{ form_end(form) }}
