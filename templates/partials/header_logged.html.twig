<header class="auth-header app-header">
    <a class="auth-brand" href="{{ path('app_home') }}" aria-label="MyLibrary Eletrônico">
        <span class="auth-brand-mark" aria-hidden="true">MLe</span>
        <span>MyLibrary Eletrônico</span>
    </a>

    <nav class="auth-nav app-nav" aria-label="Menu principal">
        <button type="button" class="app-menu-item" data-target="home">
            {{ app.user.userName }} {{ app.user.userLastName }}
        </button>
        <a href="{{ path('app_logout') }}" class="app-logout">Sair</a>
    </nav>
</header>
